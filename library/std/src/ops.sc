pub let add = core.ops.add
pub let sub = core.ops.sub
pub let mul = core.ops.mul
pub let div = core.ops.div
pub let rem = core.ops.rem
pub let neg = core.ops.neg
pub let bit_and = core.ops.bit_and
pub let bit_or = core.ops.bit_or
pub let bit_xor = core.ops.bit_xor
pub let shl = core.ops.shl
pub let shr = core.ops.shr
pub let not = core.ops.not
pub let add_assign = core.ops.add_assign
pub let sub_assign = core.ops.sub_assign
pub let mul_assign = core.ops.mul_assign
pub let div_assign = core.ops.div_assign
pub let rem_assign = core.ops.rem_assign
pub let bit_and_assign = core.ops.bit_and_assign
pub let bit_or_assign = core.ops.bit_or_assign
pub let bit_xor_assign = core.ops.bit_xor_assign
pub let shl_assign = core.ops.shl_assign
pub let shr_assign = core.ops.shr_assign
pub let eq = core.ops.eq
pub let partial_ordering = core.ops.partial_ordering
pub let partial_ord = core.ops.partial_ord
pub let index = core.ops.index
pub let chain = core.ops.chain
pub let coalesce = core.ops.coalesce
pub let unwrap = core.ops.unwrap
pub let raise = core.ops.raise

pub let add = core.ops.arith.add
pub let sub = core.ops.arith.sub
pub let mul = core.ops.arith.mul
pub let div = core.ops.arith.div
pub let rem = core.ops.arith.rem
pub let neg = core.ops.arith.neg
pub let bit_and = core.ops.bit.bit_and
pub let bit_or = core.ops.bit.bit_or
pub let bit_xor = core.ops.bit.bit_xor
pub let shl = core.ops.bit.shl
pub let shr = core.ops.bit.shr
pub let not = core.ops.bit.not
pub let add_assign = core.ops.assign.add_assign
pub let sub_assign = core.ops.assign.sub_assign
pub let mul_assign = core.ops.assign.mul_assign
pub let div_assign = core.ops.assign.div_assign
pub let rem_assign = core.ops.assign.rem_assign
pub let bit_and_assign = core.ops.assign.bit_and_assign
pub let bit_or_assign = core.ops.assign.bit_or_assign
pub let bit_xor_assign = core.ops.assign.bit_xor_assign
pub let shl_assign = core.ops.assign.shl_assign
pub let shr_assign = core.ops.assign.shr_assign
pub let eq = core.cmp.eq
pub let partial_ordering = core.cmp.partial_ordering
pub let partial_ord = core.cmp.partial_ord
pub let index = core.ops.index.index
pub let chain = core.flow.chain
pub let coalesce = core.flow.coalesce

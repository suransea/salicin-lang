let Resource = struct(value: i32)

extend Resource: Drop {
  let drop(borrow(mut) self)(): () = {
    let checked = 1 / self.value
  }
}

let main(): i32 = {
  let boxed = box_new(Resource(1))
  42
}
